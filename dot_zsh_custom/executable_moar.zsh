zi for as"null" lucid from"gh-r" \
mv'moar* moar' sbin'**/moar(.exe|) -> moar' walles/moar

if [ -x "$(command -v moar)" ]; then
    # export PAGER=$(which moar)
    export MOAR='-style solarized-dark'
fi

