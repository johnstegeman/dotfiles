#!/bin/zsh

{{ if eq .chezmoi.os "darwin" }}
sudo mv /etc/nix/nix.conf /etc/nix/nix.conf.before-nix-darwin
sudo mv /etc/zshrc /etc/zshrc.before-nix-darwin
sudo nix run nix-darwin -- switch --flake ~/.config/nix-home#"macosSystem"

{{ else }}

nix run home-manager/master -- switch --flake ~/.config/nix-home"#linuxSystem"

{{ end}}